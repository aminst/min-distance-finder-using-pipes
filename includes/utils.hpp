#ifndef __UTILS__
#define __UTILS__

#include <vector>
#include <iostream>
#include <string>
#include <sstream>
#include <cmath>

std::vector<std::string> parse_line(std::string line, std::string delimiter);
double get_distance(std::string first, std::string second);

#endif

